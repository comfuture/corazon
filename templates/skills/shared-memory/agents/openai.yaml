interface:
  display_name: "Shared Memory"
  short_description: "Search and upsert shared memory via Corazon memory APIs"
  default_prompt: "Maintain shared long-term memory using mem0 + ChromaDB through memory APIs"
